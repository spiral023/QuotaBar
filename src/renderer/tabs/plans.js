/* global QB */
'use strict';
window.QB = window.QB || {};

// Stub — wird in Task 9 durch die vollständige Abo-Verwaltung ersetzt.
QB.renderPlans = function renderPlans() {
  const c = document.getElementById('plans-content');
  if (c) c.innerHTML = '<div class="empty"><span>Abo-Verwaltung folgt…</span></div>';
};
